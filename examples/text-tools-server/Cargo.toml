[package]
name = "text-tools-server"
version = "0.1.0"
edition = "2021"
publish = false
description = "Text tools MCP server example using HTTP transport"

[dependencies]
model-context-protocol = { path = "../..", features = ["http-server", "macros"] }
tokio = { version = "1.0", features = ["full"] }
serde_json = "1.0"
